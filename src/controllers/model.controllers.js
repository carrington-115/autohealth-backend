import ScanModel from "../services/agents/autoscan.agent.js";
import CheckModel from "../services/agents/autocheck.agent.js";
import CompanionModel from "../services/agents/autochat.agent.js";

export const autoscanController = (req, res) => {
  try {
  } catch (error) {}
};

export const autocheckerController = (req, res) => {
  try {
  } catch (error) {}
};

export const autocompanionController = (req, res) => {
  try {
  } catch (error) {}
};
